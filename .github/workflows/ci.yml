name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    uses: flipslidersand-labs/qa-workflows/.github/workflows/go-test.yml@main
    with:
      go-version: "1.22"
      coverage-threshold: 0
      test-flags: "-timeout 60s"
      run-vet: true
      run-build: true
